document.addEventListener('DOMContentLoaded', () => {
  const button = document.querySelector('.transition-button');
  button.addEventListener('click', () => {
    alert('¡Botón clickeado con éxito!');
  });
});
